/*
multi-line comment
*/

// defining variables (always use let or const, avoid using var)
var name = "Rajdeep";  //function scoped
let age = 21;  //block scoped
const pi = 3.14159;

// data types - number, string, boolean, null, undefined, object, symbol

// mathematical operators - *, /, +, -, %, **, ++, --, +=, -=, *=, /=, %=, **=, etc
console.log(++age);
console.log(age);
console.log(age ++);
console.log(age);

// comparison operators - ==, ===, !=, !==, >, <, >=, <=, etc
console.log(5 === 5);    // strict equality(===), checks both value and type
console.log(5 !== "5");  // strict inequality(!==), checks both value and type
console.log(5 === "5");  

// logical operators - &&(and), ||(or), !(not)

